# Etch-A-Sketch

Second last project of Odin's Foundation Path

# Pseudocode

1. Create divs using js and putting them into a div container in HTML
2. Style into flexblox to make it look like grid 
3. Implement a hover effect that changes the color when the mouse passes through a div
4. Add button to change the number of grid